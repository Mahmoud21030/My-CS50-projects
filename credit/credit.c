#include <cs50.h>
#include <stdio.h>

void checksum(int x);
int array[16];

int main(void)
{
    int card = get_int("enter Card number");
    checksum(card);
    string x = "90";
    printf("%i",x[0]);
}

void checksum(int x)
{

return;
}
