#include <cs50.h>
#include <stdio.h>
#define NELEMS(x)  (sizeof(x) / sizeof((x)[0]))

void checksum(int x);
int array[16];

int main(void)
{
    int card = get_int("enter Card number");
    checksum(card);
    string x = "90";
    printf("%li",NELEMS(array));
}

void checksum(int x)
{

return;
}
