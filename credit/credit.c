#include <cs50.h>
#include <stdio.h>

void checksum(int x);
int

int main(void)
{
    int card = get_int("enter Card number");
    checksum(card);
    ;
    printf("%c",x[0]);
}

void checksum(int x)
{

return;
}
