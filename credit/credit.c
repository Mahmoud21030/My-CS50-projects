#include <cs50.h>
#include <stdio.h>
#define NELEMS(x)  (sizeof(x) / sizeof((x)[0]))

void checksum(int x);
int Mas[16];
int Am[15];
int vis16[16];
int vis[13];

int main(void)
{
    int card = get_int("enter Card number");
    checksum(card);

}

void checksum(int x)
{

return;
}
