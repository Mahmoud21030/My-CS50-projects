#include <cs50.h>
#include <stdio.h>

void checksum(int x);


int main(void)
{
    int card = get_int("enter Card number");
    checksum(card);
    string x = "50";
    printf("%c",x[0]);
}

void checksum(int x)
{

return;
}
