#include <cs50.h>
#include <stdio.h>
void printer(int x , int y);
int main(void)
{
    while (1)
    {
        int num = get_int("Enter the Hight: ");
        if (num < 9 && num > 0)
        {
            printf("hello world\n");
        }
        else{break;}
    }
}


void printer(int x , int y){
    for
}
